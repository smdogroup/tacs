if [ $# -lt 1 ] ; then
cat <<HELP
Converts a number of .f5 generated by TACS to .plt files

USEAGE: f5convert *.f5
HELP
  exit 0
fi
for file in $*; do
    if [ -f "$file" ] ; then
       f5totec $file
    fi
done
